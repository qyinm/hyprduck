{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default DuckDocs desktop capability for the main local window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
