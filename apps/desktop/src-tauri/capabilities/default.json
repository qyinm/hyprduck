{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default DuckDocs desktop capability for all local windows.",
  "windows": ["main", "settings", "progress", "result"],
  "permissions": ["core:default"]
}
